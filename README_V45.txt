GRIDLOCK V45 — repaired full site

- Fixes V44 fatal JavaScript syntax error in openCollegeGame(g,opts={}).
- All 13 inline scripts pass node --check.
- api/odds.js passes node --check.
- Restores College schedule, projections, value board, upset watch and live scoreboard.
- Preserves V44 possession football, current-play emphasis, expanded hero alerts and event-specific live odds lookup.
